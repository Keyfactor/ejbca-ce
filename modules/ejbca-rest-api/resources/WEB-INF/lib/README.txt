Jar files in this folder (lib) are to force old application servers (eg. JBoss EAP 6.4.0) to use the Jackson2 provider.

These jar-s can be removed if minimal version of an application server supports the Jackson2 provider by default.