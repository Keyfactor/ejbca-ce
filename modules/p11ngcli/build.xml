<?xml version="1.0" encoding="UTF-8"?>
<project name="p11ngcli" default="build">
  <description>
    Builds a utility jar containing all classes needed by CLI modules
  </description>

  <dirname property="p11ngcli.dir" file="${ant.file.p11ngcli}"/>

  <import file="${p11ngcli.dir}/../build-helpers.xml"/>

  <property name="p11ngcli.build.dir" location="${p11ngcli.dir}/build"/>
  <property name="p11ngcli.src.dir" location="${p11ngcli.dir}/src"/>

  <path id="p11ngcli.lib.classpath">
    <path location="${mod.cli-util.lib}"/>
    <path location="${mod.cesecore-common.lib}"/>
    <path refid="lib.ejbca-common.classpath"/>
    <path refid="lib.log4j.classpath"/>
    <path refid="lib.bouncycastle.classpath"/>
    <path refid="lib.commons-collections.classpath"/>
    <path refid="lib.commons-config.classpath"/>
    <path refid="lib.commons-lang.classpath"/>
    <path refid="lib.commons-logging.classpath"/>
	<path refid="lib.jna.classpath"/>
	<path refid="lib.commons-lang.classpath"/>
	<path refid="lib.jee.classpath" />
	<path refid="lib.jpa.classpath"/>
	<path refid="lib.bouncycastle.classpath"/>
	<path refid="lib.log4j.classpath"/>
	<path refid="lib.commons-logging.classpath"/>
	<path refid="lib.commons-codec.classpath"/>
	<path refid="lib.commons-config.classpath"/>
	<path refid="lib.commons-collections.classpath"/>
	<path refid="lib.commons-io.classpath"/>
	<path refid="lib.jacknji.classpath"/>
  </path>

  <target name="build" description="Build this module" depends="compile">
    <mkdir dir="${p11ngcli.build.dir}/conf"/>
    <copy todir="${p11ngcli.build.dir}/conf">
      <fileset dir="${ejbca.home}/conf" includes="ejbca.properties"/>
    </copy>
    <copy todir="${p11ngcli.build.dir}">
      <fileset dir="${ejbca.home}/src/java" includes="defaultvalues.properties"/>
      <fileset dir="${ejbca.home}/src" includes="intresources/*resources.*.properties"/>
    </copy>
    <pathconvert property="p11ngcli.dependencies" pathsep=" ">
      <path>
	<fileset dir="${mod.p11ngcli.dist}" includes="lib/*.jar"/>
      </path>
      <map from="${mod.p11ngcli.dist}/" to=""/>
    </pathconvert>

    <buildservicemanifest classpath="p11ngcli.lib.classpath" file="${p11ngcli.build.dir}" interface="org.ejbca.ui.cli.infrastructure.command.CliCommandPlugin"/>

    <jar jarfile="${mod.p11ngcli.lib}">
      <manifest >
	<attribute name="Class-path" value="${p11ngcli.dependencies} ./" />
	<attribute name="Main-Class" value="org.ejbca.ui.p11ngcli.CmpClient"/>
      </manifest>
      <fileset dir="${p11ngcli.build.dir}" includes="**/*.class **/*.xml **/*.properties META-INF/**/*"/>
    </jar>
  </target>

  <target name="clean" description="Clean up this module">
    <delete dir="${p11ngcli.build.dir}" />
    <delete dir="${mod.p11ngcli.dist}" />
  </target>

  <target name="compile" depends="setup, with.clover">
    <mkdir dir="${p11ngcli.build.dir}" />
    <javac destdir="${p11ngcli.build.dir}" debug="on" includeantruntime="no" encoding="UTF-8" target="${java.target.version}">
      <classpath>
	<path refid="lib.bouncycastle.classpath"/>
	<path refid="lib.jee.classpath"/>
	<path refid="p11ngcli.lib.classpath"/>
      </classpath>
      <src path="${p11ngcli.src.dir}"/>
    </javac>
  </target>

  <target name="setup">
    <mkdir dir="${mod.p11ngcli.dist}"/>
    <pathconvert property="lib.p11ngcli.classpath.property" pathsep=" ">
      <path refid="p11ngcli.lib.classpath" />
      <map from="${ejbca.home}/" to=""/>
    </pathconvert>
    <copy todir="${mod.p11ngcli.dist}/lib" flatten="true">
      <fileset dir="${ejbca.home}" includes="${lib.p11ngcli.classpath.property}"/>
    </copy>
    <copy file="${log4j.cli.file}" tofile="${mod.p11ngcli.dist}/log4j.xml" failonerror="true"/>
    <chmod file="${mod.p11ngcli.dist}/*.sh" perm="a+rx"/>
  </target>

</project>
