Tool for exporting the data generated by the old audit log system to file.

Run with "java -jar oldlogexport-cli.jar" for options and usage.

The used database configuration derived from EJBCA property file database.properties is available in META-INF/persistence.xml.
