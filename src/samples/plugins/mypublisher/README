Minimal EJBCA plugin for EJBCA 5
================================

This sample contains a minimal EJBCA "custom publisher".
It can be configured in the Admin GUI using the path:

       com.example.mypublisher.MyPublisher

After install, you should be able to "Save and Test Connection"
in the publisher GUI. 